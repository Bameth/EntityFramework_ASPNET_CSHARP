@* index.csthml *@
@{
    ViewData["Title"] = "Gestion des Clients";
}

<div class="bg-gradient-to-br from-indigo-200 to-blue-300 min-h-screen py-6">
    <div class="max-w-7xl mx-auto bg-white shadow-lg rounded-xl p-8">
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-5xl font-extrabold text-gray-800">Liste des Clients</h1>
            <a href="@Url.Action("Create", "Client")"
                class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-green-500 to-green-700 text-white rounded-lg shadow-lg hover:shadow-xl transition-transform transform hover:scale-105">
                Nouveau
                <svg class="h-6 w-6 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                </svg>
            </a>
        </div>

        <form method="get" action="@Url.Action("Index", "Client")"
            class="flex items-center bg-gray-100 p-4 rounded-lg shadow-inner space-x-4">
            <div class="flex-grow">
                <div class="relative">
                    <input type="text" name="telephone" placeholder="Rechercher par téléphone..."
                        class="border border-gray-300 rounded-lg pl-12 pr-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 w-full transition-all" />
                    <span class="absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400">
                        <i class="fas fa-phone-alt mt-6"></i>
                    </span>
                </div>
            </div>

            <div class="flex-grow">
                <div class="relative">
                    <input type="text" name="surname" placeholder="Rechercher par surname..."
                        class="border border-gray-300 rounded-lg pl-12 pr-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 w-full transition-all" />
                    <span class="absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400">
                        <i class="fas fa-user mt-6"></i>
                    </span>
                </div>
            </div>

            <button type="submit"
                class="px-6 py-3 bg-blue-600 text-white rounded-lg shadow-lg hover:bg-blue-700 transition-colors mt-6">
                <i class="fas fa-search mr-2 "></i> Rechercher
            </button>
        </form>

        <div class="overflow-x-auto rounded-lg shadow-lg mt-8">
            <table class="min-w-full table-auto bg-white rounded-lg">
                <thead class="bg-gradient-to-r from-blue-600 to-blue-800 text-white uppercase">
                    <tr>
                        <th class="px-6 py-3 text-left text-sm font-bold tracking-wider">#</th>
                        <th class="px-6 py-3 text-left text-sm font-bold tracking-wider">Nom et prénom</th>
                        <th class="px-6 py-3 text-left text-sm font-bold tracking-wider">Surname</th>
                        <th class="px-6 py-3 text-left text-sm font-bold tracking-wider">Téléphone</th>
                        <th class="px-6 py-3 text-left text-sm font-bold tracking-wider">Montant Due</th>
                        <th class="px-6 py-3 text-left text-sm font-bold tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="text-gray-700">
                    @foreach (var client in Model)
                    {
                        <tr class="hover:bg-gray-100 transition duration-200">
                            <td class="px-6 py-4">
                                @if (client.User != null)
                                {
                                    @client.User.Nom @client.User.Prenom
                                }
                                else
                                {
                                    @("Pas d'utilisateur")
                                }
                            </td>

                            <td class="px-6 py-4">@client.Surname</td>
                            <td class="px-6 py-4">@client.Telephone</td>
                            <td class="px-6 py-4 flex space-x-4">
                                <a href="@Url.Action("Details", "Client", new { id = client.Id })"
                                    class="text-blue-600 hover:text-blue-800 transition">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <button class="text-red-600 hover:text-red-800 transition" data-toggle="modal"
                                    data-target="#deleteModal">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @* <div class="flex justify-center mt-8">
        @if (Model.HasPreviousPage)
        {
        <a href="@Url.Action("Index", "Clients", new { page = Model.CurrentPage - 1 })" class="mx-1 px-4 py-2 border
        border-gray-300 bg-white text-gray-600 rounded-lg hover:bg-gray-200 transition">Précédent</a>
        }

        @for (int i = Model.CurrentPage - 2; i <= Model.CurrentPage + 2; i++)
        {
        if (i > 0 && i <= Model.TotalPages)
        {
        <a href="@Url.Action("Index", "Clients", new { page = i })" class="mx-1 px-4 py-2 border border-gray-300
        bg-white text-gray-600 rounded-lg hover:bg-gray-200 transition">@i</a>
        }
        }

        @if (Model.HasNextPage)
        {
        <a href="@Url.Action("Index", "Clients", new { page = Model.CurrentPage + 1 })" class="mx-1 px-4 py-2 border
        border-gray-300 bg-white text-gray-600 rounded-lg hover:bg-gray-200 transition">Suivant</a>
        }
        </div> *@
    </div>
</div>
